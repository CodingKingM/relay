#code was generated by AI
# Use JDK 21 as required by the project specifications (T-B3)
FROM eclipse-temurin:21-jdk

# Set working directory
WORKDIR /app

# Copy the JAR file (will be created during build process)
COPY target/*.jar app.jar

# Environment variables (can be overridden in docker-compose)
ENV SPRING_DATASOURCE_URL="jdbc:mariadb://db:3306/relay?createDatabaseIfNotExist=true"
ENV SPRING_DATASOURCE_USERNAME=root
ENV SPRING_DATASOURCE_PASSWORD=asdf1234

# Expose the port the app runs on
EXPOSE 8080

# Command to run the application
# The entrypoint must be configured to connect to the external MariaDB container named "db" (T-B3)
ENTRYPOINT ["java", "-jar", "app.jar"]